{
  "name": "v2-iam-companion",
  "private": true,
  "version": "1.0.0",
  "description": "Standalone mock IAM/OAuth2 authorization server for the V2 stack, backed live by the Admin Console. Extracted from the legacy V1 server so V2 (v2/, v2-backend/, v2-recommendation/) can boot with zero dependency on the V1 app.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "check": "tsc --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "4.17.21",
    "@types/node": "20.16.11",
    "tsx": "^4.19.1",
    "typescript": "5.6.3"
  }
}
