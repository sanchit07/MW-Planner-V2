package com.mw.recommendation.engine.domain;

/**
 * How the inventory was selected: AUTO = system auto-selected, MANUAL = user selected. Null = not
 * selected.
 */
public enum SelectionMode {
  MANUAL,
  AUTO
}
