package com.mw.planner.dto;

/** Enum representing the available bulk actions that can be performed on campaigns. */
public enum CampaignAction {
  /** Duplicate the selected campaigns with new names */
  DUPLICATE,
  /** Archive the selected campaigns */
  ARCHIVE,
  /** Delete the selected campaigns */
  DELETE
}
